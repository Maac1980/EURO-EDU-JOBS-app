{
  "name": "@workspace/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema/index.ts"
  },
  "scripts": {
    "push": "echo 'DISABLED: drizzle-kit push drops tables not in schema. Use CREATE TABLE IF NOT EXISTS in route files instead.'",
    "push-force": "echo 'DISABLED: --force drops production tables. NEVER use this.'"
  },
  "dependencies": {
    "drizzle-orm": "latest",
    "drizzle-zod": "^0.8.3",
    "pg": "^8.20.0",
    "zod": "latest"
  },
  "devDependencies": {
    "@types/node": "latest",
    "@types/pg": "^8.18.0",
    "drizzle-kit": "^0.31.9"
  }
}
